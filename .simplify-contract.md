# Preservation contract: whole-plugin simplify, 2026-09-10

Request: "use kernel simplify to get a cleaner result overall. for the whole plugin. and dont
touch the codex lanes."

Every line below must still be true at the end. A shorter plugin that drops one of these is a
scope cut, not a simplification.

| # | Outcome that must survive | Source | How it is checked |
|---|---|---|---|
| 1 | All 28 skills load and route | skills/, skill_signals.py | run-tests.sh, test_skill_routing.py |
| 2 | All 10 agents present and Claude-reachable | agents/ | run-tests.sh |
| 3 | Every registered hook is bound and provably able to refuse | hooks/gates.json | run-corpus.py, 4 checks |
| 4 | Tool hooks proven on both host payload shapes | gates.json host_shapes | run-corpus.py HOST SHAPES |
| 5 | Claude and Codex adapters generated and in sync | governance/hosts.json | test_adapters.py |
| 6 | Ambient session cost stays at or under 4000 tok | test_ambient_budget.py | that test |
| 7 | Version declarations stay in lockstep | bump-version.sh | test_version_sync_all |
| 8 | Documented skill/agent counts match disk | docs/ | metadata + inventory test |
| 9 | Full suite stays green (338 at start) | tests/run-tests.sh | that command |
| 10 | No gate weakens: same block/allow verdicts | corpus cases | run-corpus.py |
| 11 | No exported/public interface changes without asking | hard_rules | git diff on def/export lines |
| 12 | Codex lanes untouched: no restart, no plugin upgrade, no tag, no write into a lane dir | Aria, this request | pgrep before/after; no bump; no tag |

Deferred is not preserved. Nothing here may become "later" or "optional".
