{"date":"2026-06-27","mechanism":"runaway-agent killswitch","kind":"hook","commit":"2e85d8a","why":"Caps tripped on ordinary multi-hour sessions and the escape hatches were unreachable mid-run, so it fired mostly as a false positive.","replaced_by":"forge's max_budget_usd requirement plus spend-reservation ledgers; no standing cap.","evidence":"Removal commit message; no incident since attributable to its absence."}
{"date":"2026-07-05","mechanism":"commands layer (14 /commands)","kind":"surface","commit":"c23e6cb","why":"Two invocation surfaces for one behaviour. Skills carry frontmatter, routing, and tests; commands carried none.","replaced_by":"skills/<name>/SKILL.md, invoked as /kernel:<name>.","evidence":"Migration commit. NOTE: this migration silently orphaned the github-integration call sites, which is the erosion this ledger exists to make visible."}
{"date":"2026-07-08","mechanism":"ten domain skills (api, backend, e2e, testing, security, git, refactor, performance, quality, validate)","kind":"skill","commit":"4dfb39c","why":"Never fired across roughly 1,900 sessions; the model performs this work directly without the methodology file.","replaced_by":"Kernel 9 domain packs, loaded per classified route.","evidence":"v8.1.2 debloat measured invocation counts before removing. The only retirement in KERNEL's history that shipped with data."}
{"date":"2026-07-08","mechanism":"seven agents (validator, triage, approval-learner, analyzer, cartographer, coroner, pre-ship)","kind":"agent","commit":"4dfb39c","why":"Never fired or redundant with an existing agent.","replaced_by":"coroner's postmortem role is being absorbed by the chronicle Stop-gate (#170); pre-ship's role by the human-pass verdict gate (#179). The other five have no successor and need none.","evidence":"v8.1.2 debloat invocation counts."}
{"date":"2026-08-07","mechanism":"spec-interview recipe in skills/build","kind":"un-retirement","commit":"dd99e7d","why":"RECORDED IN REVERSE. It was lost in the v8 prose rewrite (b61ff76) with no decision, no evidence, and no successor: pure erosion. Revived and sharpened in 9.1.2.","replaced_by":"n/a - restored.","evidence":"v7.17.0:skills/build/SKILL.md line 29 held the original; HEAD kept only a vague echo until #172."}
{"date": "2026-08-08", "mechanism": "_gh_post_learning / _gh_post_decision / _gh_post_handoff (Discussions cadence posting)", "kind": "function", "commit": "#169", "why": "Cadence posting records that a session happened, not that anything changed. Every call site died with the commands layer and nobody noticed for months, which is itself the measurement: nobody was reading them.", "replaced_by": "_gh_post_landing_receipts, which fires on state changes only (work landed against an issue) and comments on that issue.", "evidence": "Zero callers since c23e6cb. matra's pilot: 29 checkpoints a day rebinding one frozen SHA recorded a stall without preventing it; comments humans will not read are rows nobody queries."}
{"date": "2026-08-08", "mechanism": "_gh_close_issue", "kind": "function", "commit": "#169", "why": "Nothing should auto-close an issue. GitHub closes it from a merged PR's Closes reference, where a human reviewed the claim first.", "replaced_by": "Closes #N in the PR body.", "evidence": "Zero callers and zero instruction references since the commands layer went."}
{"date": "2026-09-10", "mechanism": "five orphaned hook-library helpers (kernel_hook_content, kernel_hook_file_path, kernel_hook_error, kernel_semver, update_current_symlink)", "kind": "library", "commit": "pending", "why": "No call site anywhere in the repo since the 2026-08-08 baseline, tracked by #174 and #244. update_current_symlink was a one-line alias for kernel_update_current, which is the function actually wired into kernel_reconcile_runtime. Their shared parser kernel_hook_file_records is deliberately NOT retired with them: it is the only record of the captured Codex apply_patch payload shape, and is baselined as deferred instead.", "replaced_by": "Per-hook jq extraction for the payload accessors; kernel_update_current directly for the symlink alias; the runtime selector's own comparison for kernel_semver.", "evidence": "scripts/check-orphans.py listed all five as candidate-for-retirement; two tests asserted only that the alias existed and that common.sh mentioned its name, both satisfied by the definition alone, and are replaced by one test that the wired path calls kernel_update_current."}
